name: "Smart Scan - Test"
on: push
jobs:
  my_first_job:
    name: "testing"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: leftrightleft/smart-scan@main 
          
    
