name: "Smart Scan - Test"
on: push
jobs:
  my_first_job:
    name: "testing"
    runs-on: ubuntu-latest

    steps:
      - uses: leftrightleft/smart-scan@main 
        with:
          openai_api_key: ${{ secrets.OPENAI_API_KEY }}
          diff_url: ${{ github.event }}
          
    
